# ANN
Implemented a custom Artificial Neural Network using NumPy to classify Iris flowers with forward/backward propagation, softmax output, and gradient-descent weight updates. Achieved 95–98% accuracy after training on standardized features.
